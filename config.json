{
  "name": "json",
  "viewName": "JSON",
  "type": "executer",
  "main": "app.js",
  "execProgram": "node",
  "exposeRoute": false,
  "imgUrl": "logo.png",
  "version": "1.1.0",
  "description": "Kaholo plugin used to read, write, and parse JSON files",
  "category": [
    "Basic Tools"
  ],
  "keywords": [
    "json",
    "jason",
    "object",
    "document",
    "files",
    "read",
    "write",
    "parse"
  ],
  "methods": [
    {
      "name": "readJsonFile",
      "viewName": "Read JSON File",
      "params": [
        {
          "name": "filePath",
          "viewName": "File path",
          "type": "string",
          "required": true,
          "description": "A JSON file to parse and deliver in Final Result"
        }
      ]
    }, {
      "name": "writeJsonFile",
      "viewName": "Write JSON File",
      "params": [
        {
          "name": "filePath",
          "viewName": "File path",
          "type": "string",
          "required": true,
          "description": "A path and file name where the JSON document will be written to file"
        },
        {
          "name": "json",
          "viewName": "JSON Document",
          "type": "text",
          "parserType": "object",
          "required": true,
          "description": "The JSON document as multiline text, one-line string or a code layer object"
        },
        {
          "name": "echoDocument",
          "viewName": "Return Document As Final Result",
          "type": "boolean",
          "default": true,
          "description": "Also return the JSON document in the Action execution's Final Result."
        }
      ]
    }
  ]
}
